// Initial data is intentionally empty so the app starts with no hardcoded items.
// All pantry data will come from user input at runtime.
export const pantryItems = [];

// No predefined recipes; user or future AI features can populate this.
export const recipes = [];

// No historical waste trend data by default.
export const wasteTrend = [];

// Summary defaults to zero values until real data is recorded.
export const wasteSummary = {
  total: '0',
  delta: '0%',
};


